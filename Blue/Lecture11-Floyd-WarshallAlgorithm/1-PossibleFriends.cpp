#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int M;
        
        string input;
        cin >> input;
        M = input.length();
        for (int i=0; i<M; i++)
        {
            cin >> input;
        }
    }

    return 0;
}