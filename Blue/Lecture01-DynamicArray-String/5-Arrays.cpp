#include <bits/stdc++.h>
using namespace std;

int main()
{
    int nA, nB;
    cin >> nA >> nB;
    int k, m;
    cin >> k >> m;

    vector<int> A(nA);
    for (int i = 0; i < nA; i++)
    {
        int x;
        cin >> x;
        A.push_back(x);
    }

    vector<int> B(nB);
    for (int i = 0; i < nB; i++)
    {
        int x;
        cin >> x;
        B.push_back(x);
    }

    

    return 0;
}